# QueueIt - Test Credentials

## Admin Access
- **Password**: `hostel2024`
- **Auth Token**: Bearer token is the password itself: `hostel2024`
- **Login URL**: `/admin`

## Spotify
- Spotify Client ID and Secret are in `/app/backend/.env`
- Spotify requires real OAuth (dev mode - only whitelisted users)
